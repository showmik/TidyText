<UserControl x:Class="TidyText.App.Views.AIAssistantPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TidyText.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="380">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. Header (Pinned Top) -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="✨ AI Assistant" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
            
            <Border Grid.Column="1" Background="{DynamicResource PanelBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="2" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <ComboBox Width="85" ItemsSource="{Binding AvailableProviders}" SelectedItem="{Binding ActiveProviderName}" 
                              BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextMutedBrush}" Padding="4,2"/>
                    <Rectangle Width="1" Fill="{DynamicResource BorderBrush}" Margin="2,4"/>
                    <ComboBox Width="140" ItemsSource="{Binding AvailableModels}" SelectedItem="{Binding ActiveModel}" 
                              BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Padding="4,2"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 2. Main Scrollable Area (Middle) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
            <StackPanel>
                
                <TextBlock Text="Quick Actions" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextBrush}"/>
                
                <ItemsControl ItemsSource="{Binding Templates}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.ExecuteTemplateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource AIActionButtonStyle}"
                                    Effect="{DynamicResource SoftGlow}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Icon}" Margin="0,0,12,0" VerticalAlignment="Center" FontSize="16"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextBrush}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </ScrollViewer>

        <!-- 3. Custom Prompt (Pinned Bottom) -->
        <Border Grid.Row="2" Background="{DynamicResource PanelBgBrush}" Margin="-8,0,-8,-8" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Custom Prompt" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextBrush}"/>
            
            <Grid Grid.Row="1">
                <TextBox Text="{Binding CustomPrompt, UpdateSourceTrigger=PropertyChanged}" 
                         TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         Background="{DynamicResource BgBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Padding="12"
                         Foreground="{DynamicResource TextBrush}" CaretBrush="{DynamicResource TextBrush}"
                         FontFamily="Consolas" FontSize="14" />
                <TextBlock IsHitTestVisible="False" Text="Ask AI to modify text or write something..." VerticalAlignment="Top" HorizontalAlignment="Left" Margin="14,14,0,0" Foreground="{DynamicResource TextMutedBrush}" FontSize="14">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CustomPrompt}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
                     
            <Grid Grid.Row="2" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Height="4" Margin="0,0,8,4" Background="{DynamicResource BorderBrush}" Foreground="{DynamicResource AccentBrush}" Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                
                <Button Grid.Column="1" Padding="16,6" MinWidth="80">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                            <Setter Property="Content" Value="Submit"/>
                            <Setter Property="Command" Value="{Binding ExecuteCustomPromptCommand}"/>
                            <Setter Property="Effect" Value="{DynamicResource AccentGlow}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                    <Setter Property="Content" Value="Stop"/>
                                    <Setter Property="Command" Value="{Binding CancelProcessingCommand}"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#FF7B72"/>
                                    <Setter Property="BorderBrush" Value="#FF7B72"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Effect" Value="{x:Null}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            </Grid>
        </Border>

        <!-- Review Mode Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="{DynamicResource BgBrush}" 
                Visibility="{Binding IsReviewing, Converter={StaticResource BoolToVis}}" Margin="-8,-8,-8,-8" Padding="12,12,12,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Review Proposed Changes" FontSize="16" FontWeight="Bold" Margin="0,0,0,12" Foreground="{DynamicResource TextBrush}"/>
                
                <Border Grid.Row="1" Background="{DynamicResource PanelBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding DiffChunks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="chunkBorder" CornerRadius="2" Margin="0,0,0,2">
                                        <TextBlock x:Name="chunkText" Text="{Binding Text}" FontFamily="Consolas" FontSize="13" TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"/>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Type}" Value="{x:Static vm:DiffChunkType.Inserted}">
                                            <Setter TargetName="chunkBorder" Property="Background" Value="#332EA043"/>
                                            <Setter TargetName="chunkText" Property="Foreground" Value="#56D364"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="{x:Static vm:DiffChunkType.Deleted}">
                                            <Setter TargetName="chunkBorder" Property="Background" Value="#33F85149"/>
                                            <Setter TargetName="chunkText" Property="Foreground" Value="#FF7B72"/>
                                            <Setter TargetName="chunkText" Property="TextDecorations" Value="Strikethrough"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
                
                <Grid Grid.Row="2" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="❌ Reject" Command="{Binding RejectChangesCommand}" Padding="8,10" FontWeight="SemiBold"
                            Background="Transparent" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Foreground="{DynamicResource TextBrush}"/>
                    <Button Grid.Column="2" Content="✅ Accept" Command="{Binding AcceptChangesCommand}" Padding="8,10" FontWeight="SemiBold"
                            Style="{StaticResource PrimaryButtonStyle}" Effect="{DynamicResource AccentGlow}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
