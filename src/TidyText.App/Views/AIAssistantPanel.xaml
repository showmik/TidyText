<UserControl x:Class="TidyText.App.Views.AIAssistantPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="350"
             Foreground="#E0E0E0">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="✨ AI Assistant" FontSize="20" FontWeight="Bold" Foreground="#00B4D8" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="1" Text="Actions" FontWeight="SemiBold" Margin="0,0,0,10"/>
        
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Templates}" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.ExecuteTemplateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                            CommandParameter="{Binding}"
                            Background="#333333" BorderThickness="0" Margin="0,0,0,5" Padding="10,8"
                            HorizontalContentAlignment="Left" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Icon}" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" Margin="0,0,0,5" Foreground="#00B4D8"/>
            
            <TextBox Grid.Row="1" Text="{Binding ResponseContent}" 
                     TextWrapping="Wrap" AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     Background="#1E1E1E" Foreground="#D4D4D4" BorderThickness="1" BorderBrush="#444444" Padding="8" IsReadOnly="True"/>
                     
            <Button Grid.Row="2" Content="Apply to Editor" Command="{Binding ApplyResponseToEditorCommand}" 
                    Margin="0,10,0,0" Padding="10,5" Background="#0077B6" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </Grid>
    </Grid>
</UserControl>
