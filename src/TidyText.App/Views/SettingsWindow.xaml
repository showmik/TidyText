<Window x:Class="TidyText.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="650" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        FontFamily="Segoe UI, -apple-system, BlinkMacSystemFont, Roboto, sans-serif"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="48" CornerRadius="8" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Border Background="{DynamicResource BgBrush}" CornerRadius="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="{DynamicResource PanelBgBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0" IsHitTestVisible="False">
                    <TextBlock Text="⚙️ Settings" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="" Width="46" Click="Close_Click" Style="{DynamicResource WindowCloseButtonStyle}" ToolTip="Close"/>
                </StackPanel>
            </Grid>
            <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" VerticalAlignment="Bottom"/>

            <!-- Content Area -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="32,24,32,24">
                    
                    <TextBlock Text="API Providers" FontSize="18" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="Configure your AI provider access keys. Keys are securely encrypted using Windows DPAPI and stored locally." 
                               Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap" Margin="0,0,0,24" FontSize="13"/>
                    
                    <!-- Gemini Card -->
                    <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" CornerRadius="8" Background="#1E1E2E" VerticalAlignment="Top" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                <TextBlock Text="✨" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <TextBlock Text="Google Gemini" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock Text="Get your API key from Google AI Studio." Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,4,0,12"/>
                                <TextBox Text="{Binding GeminiApiKey, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,8" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- OpenAI Card -->
                    <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" CornerRadius="8" Background="#1E1E2E" VerticalAlignment="Top" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                <TextBlock Text="O" FontSize="20" FontWeight="Bold" Foreground="#10A37F" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <TextBlock Text="OpenAI" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock Text="Requires a funded OpenAI platform account." Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,4,0,12"/>
                                <TextBox Text="{Binding OpenAIApiKey, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,8" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Anthropic Card -->
                    <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" CornerRadius="8" Background="#1E1E2E" VerticalAlignment="Top" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                <TextBlock Text="A" FontSize="20" FontWeight="Bold" Foreground="#D97757" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <TextBlock Text="Anthropic Claude" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock Text="Get your API key from the Anthropic Console." Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,4,0,12"/>
                                <TextBox Text="{Binding AnthropicApiKey, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,8" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- DeepSeek Card -->
                    <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" CornerRadius="8" Background="#1E1E2E" VerticalAlignment="Top" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                <TextBlock Text="D" FontSize="20" FontWeight="Bold" Foreground="#4D6BFE" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <TextBlock Text="DeepSeek" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock Text="Cost-effective and highly capable open weights models." Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,4,0,12"/>
                                <TextBox Text="{Binding DeepSeekApiKey, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,8" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                </StackPanel>
            </ScrollViewer>

            <!-- Footer / Save Area -->
            <Border Grid.Row="2" Background="{DynamicResource PanelBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="#10A37F" FontWeight="SemiBold" VerticalAlignment="Center" TextWrapping="Wrap"/>
                    
                    <Button Grid.Column="1" Content="Save Configuration" Command="{Binding SaveKeysCommand}" Style="{DynamicResource PrimaryButtonStyle}" MinWidth="160" Padding="16,10" Effect="{DynamicResource AccentGlow}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
